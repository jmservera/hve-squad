---
bump: patch
type: Added
---

- **The repo had no Python 3 example.** Added a minimal `hello_world.py` script at the repo root that prints `Hello, World!` when run with `python3 hello_world.py` (issue #5).
